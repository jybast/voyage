{% extends 'page.html.twig' %}

{% block title %}Modifier le profil{% endblock %}

{% block body %}
<div class="container">
    <div class="formulaire_conteneur">
        <div class="formulaire_masque">
            <div class="row">
                <div class="col">
                    <div class="formulaire-logo">
                            <img src="{{ asset('assets/img/User.png')| imagine_filter('vignette') }}  " alt="Logo">
                    </div>
                </div>
                <div class="col">
                    <h1 class="formulaire_titre">Modifier le profil</h1>
                </div>
            </div>
            
   

            {{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            

            {{ include('user/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
