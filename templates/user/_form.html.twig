{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="text-center">
        <button class="btn btn-lg btn-success mt-3">{{ button_label|default('Enregistrer') }}</button>
    </div>
{{ form_end(form) }}
